@AGENTS.md

<!--
CLAUDE.md deliberately contains only the `@AGENTS.md` import so Claude Code and other
agents (e.g. Codex) share one source of truth — see AGENTS.md for the actual instructions.
The `@path` import syntax and this AGENTS.md pattern are documented at:
https://code.claude.com/docs/en/memory#agents-md
Block-level HTML comments like this are stripped before CLAUDE.md loads into context,
so this maintainer note costs no tokens. It stays visible when the file is opened directly.
-->

